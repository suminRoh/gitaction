name: helloGithubAction

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    #run 뒤에는 실제 작동하는 코드를 넣어야 함 

    - name: hello world 출력
      run: echo Hello, world!

    - name: 디렉토리 출력
      run: ls -al


